pipeline {
    agent any

    environment {
        DOCKER_IMAGE = "branson404/project-django"
    }

    stages {
        stage('Git checkout') {
            steps {
                git branch: 'main', url: 'https://github.com/branson404/project-django'
            }
        }

        stage('SonarQube') {
            steps {
                script {
                    def sonarScanner = tool 'SonarScanner'
                    withSonarQubeEnv('sonarqube-credentials') {
                        sh "${sonarScanner}/bin/sonar-scanner -Dsonar.projectKey=project-django"
                    }
                }
            }
        }

        stage('Build Docker image') {
            steps {
                sh 'docker build -t $DOCKER_IMAGE .'
            }
        }

        stage('Run Docker Container') {
            steps {
                sh 'docker rm -f main_line || true' // optional cleanup before running
                sh 'docker run -d -p 8000:8000 --name main_line $DOCKER_IMAGE'
            }
        }

        stage('Docker login') {
            steps {
                withCredentials([usernamePassword(credentialsId: 'dockerhubCred', usernameVariable: 'DOCKER_USER', passwordVariable: 'DOCKER_PASS')]) {
                    sh 'echo "$DOCKER_PASS" | docker login -u "$DOCKER_USER" --password-stdin'
                }
            }
        }

        stage('Docker push') {
            steps {
                sh 'docker push $DOCKER_IMAGE'
            }
        }
    }
}
